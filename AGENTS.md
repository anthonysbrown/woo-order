# AGENTS.md

## Cursor Cloud specific instructions

This repository (`woo-order`) is currently an empty scaffold with only a `README.md`. There are no application services, dependencies, or build steps to run.

### Environment
- No runtime dependencies required (no `package.json`, `requirements.txt`, etc.).
- No services to start or databases to configure.
- The repository is git-initialized with a single `main` branch.

### When code is added
Once application code is introduced, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run lint, tests, and the dev server.
- Any non-obvious startup caveats or environment variables required.
